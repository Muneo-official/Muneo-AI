ANTHROPIC_API_KEY=
MONGO_URI=

# cross-encoder 리랭킹 사용 여부 (기본 false).
# true로 켜면 약 2.2GB짜리 리랭커를 추가로 로드하므로 워커당 RAM이 3~3.5GB로 늘어난다.
# 이미지에 리랭커를 굽지 않았다면(--build-arg USE_RERANKER=true 없이 빌드) 기동 시 다운로드가 발생한다.
USE_RERANKER=false
